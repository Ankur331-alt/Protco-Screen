package com.smart.device.listener;

import android.view.View;

/**
 * @author edwin
 */
public interface OnPinCodeListener {

    /**
     * Invoked when the pin code is selected
     * @param view the view
     */
    void onPinCode(View view);
}
